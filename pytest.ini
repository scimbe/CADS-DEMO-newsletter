[pytest]
pythonpath = .
markers =
    live: hits a real external service (Open-Meteo and/or litellm-proxy). Excluded from the default run; run explicitly with `pytest -m live`.
addopts = -m "not live"
